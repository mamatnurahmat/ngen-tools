apiVersion: v1
kind: Service
metadata:
  name: argoapi
  labels:
    app: argoapi
spec:
  type: ClusterIP
  ports:
    - port: 8899
      targetPort: 8899
      protocol: TCP
      name: http
  selector:
    app: argoapi
