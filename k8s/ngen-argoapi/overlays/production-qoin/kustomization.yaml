apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: argocd-production-qoin-

namespace: default

commonLabels:
  app: argocd-production-qoin

patches:
  - path: patch-deployment.yaml
