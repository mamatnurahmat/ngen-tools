apiVersion: apps/v1
kind: Deployment
metadata:
  name: argoapi
spec:
  template:
    spec:
      containers:
        - name: argoapi
          env:
            - name: ARGOAPI_VERSION
              value: "latest"
            - name: ROOT_PATH
              value: "/api/v1/namespaces/default/services/http:argocd-production-qoin-argoapi:8899/proxy"
            - name: ARGOCD_URL
              value: "https://193.5.2.100"
            - name: ARGOCD_USER
              value: "admin"
            - name: ARGOCD_PASSWORD
              value: "argoqoin"
