apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: argocd-staging-qoin-

namespace: default

commonLabels:
  app: argocd-staging-qoin

patches:
  - path: patch-deployment.yaml
